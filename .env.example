# Jellyfin Docker Configuration
# Copy this file to .env and adjust values as needed

# User/Group IDs for file permissions
# Use `id -u` and `id -g` to get your current user's IDs
PUID=1000
PGID=1000

# Timezone (e.g., America/New_York, Europe/London, Asia/Jerusalem)
TZ=UTC

# Web interface port
JELLYFIN_PORT=8096

# HTTPS port (optional)
JELLYFIN_HTTPS_PORT=8920

# =============================================================================
# MEDIA PATHS CONFIGURATION
# =============================================================================
# Media paths are configured directly in docker-compose.yml
# This file cannot easily handle multiple dynamic volume mounts.
#
# To add media paths:
# 1. Edit docker-compose.yml
# 2. Add volume mounts under the jellyfin service volumes section
#
# Example volume mount format:
#   - /host/path:/media/category_name:ro
#
# WSL path examples:
#   - /mnt/c/Users/YourName/Videos:/media/videos:ro
#   - /mnt/d/Movies:/media/movies1:ro
#   - /mnt/e/Movies:/media/movies2:ro
#   - /mnt/d/TVShows:/media/tvshows:ro
#
# The :ro suffix makes the mount read-only (recommended for media)
# =============================================================================
